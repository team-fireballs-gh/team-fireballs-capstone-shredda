import React from "react";
import { StyleSheet, Text, SafeAreaView } from "react-native";
import PeopleList from "../components/PeopleList";

export default function PeopeProfile () {
  return (
    <SafeAreaView>
        <PeopleList />
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});
